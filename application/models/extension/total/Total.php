<?php
class Total extends CI_Model{
	public function getTotal($total) {
		 
		 /*
		$total['totals'][] = array(
			'code'       => 'total',
			'title'      => 'Total',
			'value'      => max(0, $total['total']),
			'sort_order' => $this->configmodal->get('total_sort_order')
		);
		*/ 
	}
}