<?php
	class Sub_total extends CI_Model{
	public function getTotal($total) {
		 
 	/*	 
		 $sub_total = $this->customercartordermodal->getSubTotal();
		
		if (!empty($this->session->userdata['vouchers'])) {
			foreach ($this->session->userdata['vouchers'] as $voucher) {
				$sub_total += $voucher['amount'];
			}
		}
	 	
		$total_temp = array(
			'code'       => 'sub_total',
			'title'      => 'Sub Total',
			'value'      => $sub_total,
			'sort_order' => $this->configmodal->get('sub_total_sort_order')
		);
		 
		 $total['total'] += $sub_total;
		*/ 
	}
}
